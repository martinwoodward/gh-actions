# Basic Echo GitHub Action

Simple action to dump the contents of the environment variables and JSON payload.

## Usage

```

 action "Azure CLI" {
  uses = "martinwoodward/gh-actions/echo@master"
  env = {
  }
}

```

